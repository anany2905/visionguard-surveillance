# VisionGuard - Production Requirements
# Core dependencies only - let pip resolve ML sub-dependencies

Flask
Flask-CORS
gunicorn

opencv-python-headless
numpy
Pillow

--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0+cpu
ultralytics

deepface
python-dotenv

